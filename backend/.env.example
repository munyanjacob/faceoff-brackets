# Supabase — Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Supabase — Settings → Database
DATABASE_URL=postgresql://postgres.your-project-ref:your-db-password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.your-project-ref:your-db-password@aws-0-region.pooler.supabase.com:5432/postgres

# Round-advancement cron endpoint auth (any random string)
CRON_SECRET=generate-a-random-string

# Signs the anonymous-voter cookie (issue #35, any random string) so it
# can't be hand-edited to reset a voter's identity. Optional locally - an
# unset value falls back to an insecure dev-only default with a console
# warning (see src/app/brackets/[id]/voter-identity.ts) - but should be set
# in any real/deployed environment.
VOTE_COOKIE_SECRET=generate-a-random-string

# The frontend's exact origin, allowed by the REST API's CORS policy
# (issue #50, docs/frontend-rework-specification.md §6). Optional locally -
# an unset value falls back to the spec's placeholder production origin
# (https://app.example.com, see src/lib/api/cors.ts) - but should be set to
# the real per-environment frontend origin once one exists.
FRONTEND_ORIGIN=https://app.example.com

# The `Domain` attribute set on the signed `voter_id` cookie by
# POST /api/matchups/[matchupId]/votes (issue #56,
# docs/frontend-rework-specification.md §6/§48's shared-parent-domain
# decision) - the leading dot is what lets the cookie travel to both the
# frontend's and backend's subdomains. Optional locally - an unset value
# falls back to the spec's placeholder parent domain (.example.com, see
# src/app/api/matchups/[matchupId]/votes/route.ts) - but should be set to
# the real per-environment parent domain once one exists.
VOTER_COOKIE_DOMAIN=.example.com
