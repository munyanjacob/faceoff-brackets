/**
 * Minimal placeholder for `/dashboard` - just enough for the shared layout's
 * auth guard and "Log out" control (`src/app/dashboard/layout.tsx`) to have
 * somewhere to render. The real dashboard content is #8's scope.
 */
export default function DashboardPage() {
  return <h1 className="text-2xl font-semibold">Dashboard</h1>;
}
